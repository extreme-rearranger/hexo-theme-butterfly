.post_share
  if theme.sharejs.enable
    include ./share-js.pug
  else if theme.addtoany.enable
    //- changed to cache false since the content should be different among language settings
    !=partial('includes/third-party/share/addtoany', {}, {cache: false})
